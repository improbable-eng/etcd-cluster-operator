apiVersion: apps/v1
kind: Deployment
metadata:
  name: etcd-cluster-operator
  namespace: default
  labels:
    control-plane: etcd-cluster-operator
spec:
  selector:
    matchLabels:
      control-plane: etcd-cluster-operator
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: etcd-cluster-operator
    spec:
      containers:
      - command:
        - /manager
        args:
        - --enable-leader-election
        image: controller:latest
        name: manager
        resources:
          limits:
            cpu: 100m
            memory: 30Mi
          requests:
            cpu: 100m
            memory: 20Mi
      terminationGracePeriodSeconds: 10
