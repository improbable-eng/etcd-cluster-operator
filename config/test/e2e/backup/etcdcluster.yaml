apiVersion: etcd.improbable.io/v1alpha1
kind: EtcdCluster
metadata:
  name: e2e-backup-cluster
spec:
  replicas: 1
  storage:
    volumeClaimTemplate:
      storageClassName: standard
      resources:
        requests:
          storage: 100Mi
