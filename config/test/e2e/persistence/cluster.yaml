apiVersion: etcd.improbable.io/v1alpha1
kind: EtcdCluster
metadata:
  name: cluster1
spec:
  replicas: 1
  storage:
    volumeClaimTemplate:
      storageClassName: standard
      resources:
        requests:
          storage: 50Mi
