apiVersion: etcd.improbable.io/v1alpha1
kind: EtcdCluster
metadata:
  name: cluster1
spec:
  replicas: 1
  storage:
    volumeClaimTemplate:
      storageClassName: standard
      resources:
        requests:
          storage: 1Mi
  podTemplate:
    resources:
      requests:
        cpu: 200m
        memory: 100Mi
      limits:
        cpu: 200m
        memory: 100Mi
