
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  name: etcdpeers.etcd.improbable.io
spec:
  group: etcd.improbable.io
  names:
    kind: EtcdPeer
    listKind: EtcdPeerList
    plural: etcdpeers
    singular: etcdpeer
  scope: ""
  validation:
    openAPIV3Schema:
      description: EtcdPeer is the Schema for the etcdpeers API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: EtcdPeerSpec defines the desired state of EtcdPeer
          properties:
            bootstrapPeers:
              description: BootstrapPeers is the list of all other peers for bootstrap
                purpses. This does not need to be kept up to date after bootstrap
                has occured as it's *only* used before cluster peers have conatacted
                each other.
              items:
                description: BootstrapPeer describes a single *other* peer that will
                  be used for bootstrapping
                properties:
                  host:
                    description: Host forms part of the Advertise URL - the URL at
                      which this peer can be contacted. The port and scheme are hardcoded.
                    type: string
                  name:
                    description: Name is the initial name of the peer, which is set
                      into etcd at the end of this process
                    type: string
                required:
                - host
                - name
                type: object
              type: array
          type: object
        status:
          description: EtcdPeerStatus defines the observed state of EtcdPeer
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
